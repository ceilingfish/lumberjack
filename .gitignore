# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/

# Test binaries, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.out
coverage.html

# Normalised lcov halves and the merged report `scripts/coverage.sh` gates on
coverage-go.lcov
coverage-swift.lcov
coverage.lcov

# Go workspace file
go.work
go.work.sum

# Dependency directories (when vendoring)
vendor/

# Environment variable files
.env

# Editor / OS cruft
.DS_Store
*.swp

# Agent skills installed from skills-lock.json via `mise run skills`
/.agents/
/.claude/

# Swift build artifacts for the macOS menu-bar app (macos/)
macos/.build/
macos/.swiftpm/
