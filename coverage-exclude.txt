# Coverage exclusion list, applied to both codebases by the per-package
# coverage gate (scripts/coverage-gate.ps1).
#
# One glob pattern per line. Blank lines and "#" comments are ignored.
# A leading "/" anchors the pattern to the repository root; patterns with no
# "/" match a file's base name at any depth; patterns with an interior "/"
# match the full path relative to the repository root, where "**" matches zero
# or more path segments. Every entry must carry a justifying comment (see
# issue #6).

# Three statements, a deliberate one-liner delegating to cmd.Execute().
# Kept trivial by convention; testing it tests Cobra. Anchored to the root so
# it does not also exempt every other main.go in the repository.
/main.go

# Zero coverable statements: a package comment and a single go:embed
# variable.
internal/database/migrations/embed.go

# Generated by `buf generate`, never hand-edited.
**/*.pb.go

# Generated Swift protobuf stubs, the direct parallel to *.pb.go.
**/*.pb.swift

# Generated Swift gRPC stubs, the direct parallel to *.pb.go.
**/*.grpc.swift

# SwiftUI view bodies are not unit-testable in isolation. Logic belonging
# under test should be moved out of the view rather than exempted.
MenuBarView.swift
