# Coverage exclusion list, shared by the Go coverage gate
# (scripts/coverage.sh) and the Swift coverage mechanism.
#
# One glob pattern per line. Blank lines and "#" comments are ignored.
# Patterns with no "/" match a file's base name at any depth; patterns with
# a "/" match the full path relative to the repository root, where "**"
# matches zero or more path segments. Every entry must carry a justifying
# comment (see issue #6).

# Three statements, a deliberate one-liner delegating to cmd.Execute().
# Kept trivial by convention; testing it tests Cobra.
main.go

# Zero coverable statements: a package comment and a single go:embed
# variable.
internal/database/migrations/embed.go

# Generated by `buf generate`, never hand-edited.
**/*.pb.go

# Generated Swift protobuf stubs, the direct parallel to *.pb.go.
**/*.pb.swift

# Generated Swift gRPC stubs, the direct parallel to *.pb.go.
**/*.grpc.swift

# SwiftUI view bodies are not unit-testable in isolation. Logic belonging
# under test should be moved out of the view rather than exempted.
MenuBarView.swift
